# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Rust build artifacts (any crate in the workspace)
target/

# Local workspace extras
.workbuddy/
*.msi

# Mobile icon sets (desktop-only app)
src-tauri/icons/android/
src-tauri/icons/ios/

# Android 脚手架（src-tauri/gen/android）随源码入库 —— gradle 脚本、Manifest、
# MainActivity 这些手改后会被 `tauri android init` 覆盖，必须进版本库；目录内
# 自带的 .gitignore 已经排掉了 .gradle/ 与 build/。
# capability 的 JSON schema 由 tauri 构建期生成，仅供编辑器补全，不入库。
src-tauri/gen/schemas/
