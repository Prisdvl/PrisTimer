# 更新日志

本项目的所有显著变更都记录在此文件中。
格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [未发布]

### 新增

- **系统信息面板**：窗口底部状态栏与托盘（悬浮提示 + 右键菜单）展示已连接蓝牙设备的名称、类型与电量 —— BLE 走 GATT 电池服务、经典蓝牙走系统元数据，读不到时显示"电量未知"而不是 0；以及 opencode Go 订阅的三档配额窗口（滚动 / 本周 / 本月）剩余百分比与重置时间。配额端点已内置，填 API Key 即可用
- **托盘悬浮信息窗**：无边框置顶小窗贴托盘常驻展示，程序最小化时也能看到
- **蓝牙设备类型图标与排序**：每台设备带类型图标（耳机 / 键盘 / 鼠标 / 通用，内联 SVG 线性图标），并按"音频优先"排序 —— 耳机剩多少电是最该一眼看到的，现在固定排最前
- **配额详情浮层**：状态栏额度区只留"本月"一档，点击展开浮层显示滚动 / 本周 / 本月三档的进度条与各自重置时刻，带弹出动画
- **迷你组件显示环境信息**：缩小至组件后同样看得到已连接设备的电量与本月额度（图标 + 数字的极简胶囊，悬停给全名）；与主界面状态栏、托盘悬浮窗共用同一份数据，三处永远同步

### 修复

- **番茄钟自定义时长不生效**：计时引擎的时长上限此前只在空闲态接受修改，专注 / 休息进行中改配置被静默忽略，直到下一阶段才生效；现改为全状态生效 —— 进行中的阶段保留已用时长、按新上限重算剩余，设置面板"即改即见"。同一修复也让自由计时中途修改上限即时生效
- **配额查询恒 404**：默认指向 opencode Go 的用量端点（`/zen/go/v1/usage`）；用户填了网关根路径（少 `/usage`）时自动补全，自建网关地址仍原样保留
- **配额保存提示语义颠倒**：查询成功时弹普通提示、查询失败反而弹绿色"成功"；现按"配置已存 / 查询未成功"分别提示
- **日志并发写坏**：多线程同时追加日志会互相穿插（实测出现 `[[时间戳时间戳] 电量索引…` 的损坏行）；现加锁串行化写入
- **蓝牙扫描风暴**：节流闸门只判时间间隔、不防并发，两个窗口的兜底轮询叠加后台定时任务会让同一秒跑出多次扫描（实测 10 次，与"避免高频扫描卡死适配器"直接冲突）；现单飞串行化，真实扫描恒为每 30 秒一次
- **悬浮窗重复轮询**：悬浮信息窗与主窗口各自每秒兜底轮询，蓝牙请求整体翻倍；现仅主窗口轮询，浮窗只订阅事件
- 尚未查询时配额栏的悬停提示显示"29000 小时前"，现显示"尚未查询"
- **蓝牙耳机电量恒显示"未知"**：读取电量的系统属性键**字面量写错了三处**（GUID 后三段、属性 ID、分隔符），且只扫设备顶层节点 —— 而耳机电量挂在免手持（HFP）子节点上，于是永远读不到。现按正确键扫描全部设备节点，耳机（EDIFIER MT6）实测读出 70%
- **已断开设备仍带电量显示**：一条"补漏"通道会把仅存于系统记录、当前并未连接的设备塞进列表；现严格按连接状态过滤，只显示当前真正连接的设备
- **配置额度与启动时闪黑色命令窗口**：额度查询此前 spawn 系统 `curl.exe`，而 Tauri 是无控制台的 GUI 进程，Windows 会为子进程新建控制台窗口（实测启动时连闪两三个）；现改为 WinHTTP 原生实现，**零子进程**，黑窗从根上消失。顺带修掉一处 WinHTTP 头部长度计算 bug（多算了字符串结尾的 NUL，使带自定义头的请求一律 `ERROR_INVALID_PARAMETER(87)`）—— 这正是当年误判"本机 WinHTTP 不可用"、退回 curl 子进程的真因
- **配额 401 的提示文案**：原显示"API Key 无效"，但实测同一把 Key 在 `/zen/v1/chat/completions` 上能通过鉴权（返回 400 而非 401），凭据本身有效，只是额度端点拒绝访问；断言"Key 无效"会把用户引向错误的排查方向，现改为中性的"额度不可用（401）"
- **迷你态几何动画被丢弃**（本轮引入、当场发现并修掉）：`DWMWCP_DONOTROUND` 若排在几何动画**之前**，窗口会立刻按矩形重算 frame，紧随其后的 `SetWindowPos` 全部被丢弃 —— 症状是模板切成了迷你态、窗口却纹丝不动，内容错乱地摊在大窗口的左上/右上角。现改为几何摆好之后再定圆角
- **`SetWindowPos` 静默失败**：动画循环此前不检查返回值，"窗口没动"这类故障完全无从归因；现一次都没成功即上报错误

### 变更

- 悬浮信息窗尺寸 280×108 → 300×186：容纳多设备与三个配额窗口，不再裁切内容
- **状态栏改为紧凑形态**：设备由"完整名称 + 类型标签"压缩为"图标 + 智能简称 + 电量"（品牌前缀保留、连字符取后段、超长省略），额度区由三档并排压缩为单档"本月"，整体宽度大幅缩短
- **悬浮信息窗版式重排**：本月额度为主体（大号百分比 + 进度条 + 重置时刻），其余窗口压成一行小字，底部为设备胶囊；圆角与玻璃变量与主界面「开始」按钮统一为同一套语言
- **形态切换更跟手（关键路径 552ms → 约 470ms）**：三处叠加 —— ① 内容淡出 0.19s → 0.11s 并彻底停绘（`visibility` 延迟隐藏：几何动画期间窗口尺寸每帧在变，WebView2 要整块重采样渲染表面，一棵仍在合成的大子树会白吃每帧预算）；② 迷你形态的 4 项窗口属性（实测约 42ms）由"淡出之后串行"改为"与淡出并行" —— 它唯一的硬约束是必须早于几何动画（resizable / 最小尺寸会夹住 setSize），与内容淡出毫无依赖；③ 还原时常规最小尺寸的恢复后置到几何动画之后（把 760×560 的约束加到一个 264×96 的窗口上，Windows 会按新约束立即重排，表现是"动画还没开始窗口先跳回常规大小"）
- **迷你组件圆角 8px → 16px**：DWM 只提供 ROUND(≈8px) / ROUNDSMALL(≈4px) 两档且写死不可调，迷你态改用 `DWMWCP_DONOTROUND` 把圆角完全交给 CSS（窗口本身 `transparent`，圆角之外就是透明像素），与悬浮信息窗同半径，两个"小窗"终于是一套语言
- **迷你组件环境信息错峰入场**：时间 / 状态 / 信息胶囊 / 按钮依次淡入上浮，替代整块同时出现

## [0.4.0] — 2026-09-13

### 新增

- **沉浸模式**：一键隐藏全部外壳组件，仅保留时钟与背景动效；右上角幽灵退出键（平时近乎隐形，悬停显形），Esc 亦可退出；状态持久化，开启时自动回到计时页
- **界面控制台**：右下角玻璃圆钮展开控制台，四个独立开关自由隐藏标签栏 / 预设区 / 侧栏 / 底部提示条，选择持久化

### 变更

- **图表与标签配色全面主题化**：新增恒定强调色令牌 `--ui-accent`（浅色主题自动换深色），修复空闲态下折线图 / 柱状图 / 热力图 / 标签选中态呈灰色难以辨识的问题；连带清理四处硬编码色值
- **运行态时钟数字浅色主题压深**（`--clock-running` 令牌）：晨雾 / 纸墨主题下运行数字不再刺眼
- **标签选中动画**：弹跳入场 + 前导圆点，选中状态一眼可辨
- **应用图标定稿**：容器定形为正圆，黄金角弧与同宽双环几何不变

### 修复

- **窗口几何动画流畅度**：废弃双重节流改为纯 vsync 对齐步进，缓动改 easeInOutCubic 消除起步猛拽，整像素零位移帧跳过提交；窗口动画期间暂停背景烟层重绘

## [0.3.1] — 2026-09-13

### 变更

- **应用图标重设计（几何之美）**：容器改为真·超椭圆 squircle（连续曲率，iOS 图标同款几何）；进度弧精确扫过黄金角 137.5°；轨道与弧统一为单一线宽、圆帽齐平零台阶；轨道 / 弧 / 中心点三元素同心，全部比例锚定主环半径。小尺寸（16px）实测可辨识

## [0.3.0] — 2026-09-13

### 新增

- **简约模式**：主题菜单底部新增开关，一键关闭背景动效（烟团 / 星尘 / 流星 / 极光帷幕）与盘内粒子，保留玻璃质感与扫光；选择持久化，与主题互相独立
- **全新应用图标**：极简表盘 —— 深底、极细轨道环、一段进度弧与中心点；无指针、无刻度

### 修复

- **浅色主题运行态辨识度**：纸墨主题运行指针与粒子改浓墨黑（老大指定），晨雾改深墨绿 —— 原状态绿在浅底上对比仅 1.7:1，几乎隐形；深色主题行为不变

## [0.2.0] — 2026-09-13

### 新增

- **应用内快捷键**：窗口聚焦时空格 = 开始/暂停，Esc = 退出迷你态 / 关设置面板；输入框聚焦时自动让路，修饰键组合不劫持（刻意不做系统级全局热键）
- **SQLite 自动备份**：每日首次启动用 `VACUUM INTO` 产出一致性快照到 `backups/`，保留最近 7 份；失败仅记日志，绝不阻断启动
- **发布流水线**：`release.yml` —— 推送 `v*` 标签自动构建 MSI 并发布 GitHub Release
- 英文版 README（`README.en.md`）与下载指引

### 工程化

- 顶层 Cargo workspace 合并，前端 App.vue / 后端 lib.rs 模块化拆分（视图组件、composable、commands 分域）
- CI 门禁：Rust 测试 + clippy `-D warnings` + vue-tsc + vitest

## [0.1.0] — 首个公开版本

### 计时引擎（pristimer-core）

- 锚点计时模型：`elapsed = accumulated + (now − anchor)`，拒绝累加式计数，杜绝漂移
- 双时钟设计：`Instant` 保证进程内精度，`SystemTime` 负责落库与跨进程恢复
- 崩溃安全：WAL 持久化锚点；意外退出后恢复幂等（超 12h 按异常结束，<10s 会话丢弃）
- 倒计时封顶 limit_ms；番茄循环推进在引擎侧完成

### 界面（Vue 3 + 液态玻璃设计系统）

- 液态玻璃设计系统：四向边缘高光 + 底部折射暗影 + accent 环境色边缘光、周期性镜面扫光
- 六套主题（深空 / 虚空 / 晨雾 / 极光 / 暮霞 / 纸墨），`@property` 颜色插值 0.9s 丝滑过渡，每套主题专属背景动效；表盘指针、刻度与盘内粒子同主题换色（前景与底色明度反向）
- 迷你置顶组件：264×96 常驻桌面，原生 `SetWindowPos` 分帧动画（≤67fps，DwmFlush 对齐）平滑缩放
- 表盘粒子：运行时盘内漂移连线，出界沿法线反弹，颜色随主题
- 霓虹按钮多层光晕呼吸；顶部 2.5px 滚动进度条替代右侧滚动条
- 统计视图：年度热力图 / 小时分布 / 趋势曲线，数字滚动补间，浅色主题全量配色适配
- 科目标签：预设 + 自定义、内联重命名、每日目标
- 番茄钟：阶段条 + 可拖拽循环序列卡（拖「长休息」改轮数）+ 到点系统通知

### 持久化与数据（pristimer-store）

- SQLite（rusqlite bundled，WAL 模式）单表会话 + settings 表
- CSV 导出（明细与汇总两式，文件名净化防路径穿越）
- 科目日聚合 / 小时分布 / 年度热力数据

### 工程化

- 单实例守卫（`CreateMutexW` + 前台窗口拾取）、启动 panic 兜底对话框、三段重试开库
- GitHub Actions CI：Rust 测试 + clippy `-D warnings` 门禁 + vue-tsc + vitest
- 前端 vitest 14 例（date / tags / useCountUp）；Rust 测试 50 例
- 顶层 Cargo workspace（core / store / src-tauri 统一 lock 与 target）

[Unreleased]: https://github.com/Prisdvl/PrisTimer/compare/v0.4.0...HEAD
[0.4.0]: https://github.com/Prisdvl/PrisTimer/compare/v0.3.1...v0.4.0
[0.3.1]: https://github.com/Prisdvl/PrisTimer/releases/tag/v0.3.1
[0.3.0]: https://github.com/Prisdvl/PrisTimer/releases/tag/v0.3.0
[0.2.0]: https://github.com/Prisdvl/PrisTimer/releases/tag/v0.2.0
[0.1.0]: https://github.com/Prisdvl/PrisTimer/releases/tag/v0.1.0
